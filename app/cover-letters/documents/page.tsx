import DocumentsManager from "@/components/coverletters/DocumentsManager";

export const metadata = {
  title: "Resumes — Cover Letter Studio",
};

export default function CoverLettersDocumentsPage() {
  return <DocumentsManager />;
}
