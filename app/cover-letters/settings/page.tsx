import SettingsForm from "@/components/coverletters/SettingsForm";

export const metadata = {
  title: "Settings — Cover Letter Studio",
};

export default function CoverLettersSettingsPage() {
  return <SettingsForm />;
}
